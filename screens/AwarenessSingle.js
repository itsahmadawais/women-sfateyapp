import React from "react";
import {
    View,
    Pressable,
    StyleSheet,
    Modal,
    Alert,
    StatusBar,
    SafeAreaView,
    Text,
    ScrollView,
    FlatList,
    Image
} from 'react-native';
import AwarenessSingle from '../components/AwarenessSingle';

const AwarnesSingleScreen=()=>{
    return (
        <>
            <AwarenessSingle/>
        </>
    )
}

export default AwarenessSingle;